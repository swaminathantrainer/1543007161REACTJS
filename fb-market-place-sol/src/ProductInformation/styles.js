import { colors, fonts, margin } from '../styles/base';

export default {
  infoTextStyle: {
    color: colors.lightTextColor,
    'font-size': fonts.sm,
    margin: margin.sm
  },

  normalTextStyle: {
    color: colors.primaryTextColor,
    'font-size': fonts.md,
    margin: margin.sm
  }
}